/**
 * Main Javascript for DJO Identity Provider
 *
 * @author Roelof Roos
 */

// Always use IIFE
(function () {
  'use strict'

  let init = () => {

  }

  document.addEventListener('DOMDocumentReady', init)
}())
